name: Update Anime Data

on:
  workflow_dispatch: # Allows the workflow to be manually triggered

jobs:
  update-data:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '14'

      - name: Install dependencies
        run: npm install axios he

      - name: Fetch and Update Anime Data
        run: node fetch_and_update_data.js

        - name: Commit changes
        run: |
          git config --local user.email "actions@github.com"
          git config --local user.name "GitHub Actions"
          git add anime_data.json
          git commit -m "Update anime data"
      
          # Using PAT for authentication
          git push https://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@github.com/gojo22k/Database.git HEAD:main
      